API:-/faceScan-service/api/v1/faceScan/history-->create api based on this prompt

Entity:- 
- Create a class class named `StoreFaceScanResultModel` mapped to the `MEDGO_FACE_SCAN_RESULTS` table.
- The class should extend `BaseEntity` to inherit common auditing fields and primary key handling.
- Annotate the class with `@Entity` and `@Table(name = "MEDGO_FACE_SCAN_RESULTS")`.
- Include the following fields with proper JPA annotations:
    - `sessionId` of type `String`, annotated with `@Column(name = "SESSION_ID", nullable = false, unique = true)`, representing a unique session ID.
    - `fedId` of type `String`, representing the federated membership code.
    - `memberCode` of type `String`, representing the associated member code.
    - `scanResult` of type `String`, representing the scan result (e.g., "SUCCESS", "FAILED").
    - `scanData` of type `String`, annotated with `@Column(name = "SCAN_DATA", columnDefinition = "TEXT")`, storing JSON as a string.
    - `endTime` of type `LocalDateTime`.
    - `measurementID` of type `String`, annotated with `@Column(name = "MEASUREMENT_ID")`.
- Include a method `getScanDataAsJson()` to convert `scanData` JSON string to `Map<String, Object>` with error handling and logging.
- Include a constructor that accepts `fedId` and `memberCode`.
- Use Lombok annotations `@Data`, `@NoArgsConstructor`, `@AllArgsConstructor`, `@ToString` and logging annotation `@Slf4j`.
- Ensure all fields and inherited properties are correctly mapped to the database columns.

Request:-
create a request class named FaceScanHistoryRequest in the request package.
Implement a request DTO FaceScanHistoryRequest to receive and validate input for fetching face scan history.
Requirements:
 - Annotate the class with Lombok annotations @Data, @NoArgsConstructor, and @AllArgsConstructor.
 - Include a single field:
 - memberCode of type String, annotated with @NotBlank(message = "Member code is required") for validation.
 - This class should extend no other class (plain DTO).
 - It will be used in the controller to validate the member code input before querying face scan history.

Controller:-
Create a POST API endpoint /history in the controller.
The endpoint should:
- Accept a request body of type FaceScanHistoryRequest.
- Use @Valid for validation and @DecryptBody(FaceScanHistoryRequest.class) to decrypt the request.
- Be annotated with @EncryptResponse to encrypt the response.
- Call the faceScanService.getFaceScanHistory(request) service method.
- Return a CommonResponse object.
- This endpoint will be used to fetch the face scan history for a member.

Service Interface:-
- Create a method named getFaceScanHistory in the FaceScanService interface.
- The method should:
- Accept a single parameter of type FaceScanHistoryRequest annotated with @Valid for validation.
- Return a CommonResponse object.
- This method will be used to fetch the face scan history for a member based on the request data.

ServiceImpl:-
create the implementation of the getFaceScanHistory method in the FaceScanServiceImpl class.
Implement the getFaceScanHistory method in the FaceScanServiceImpl class.
The method should:
- Accept a FaceScanHistoryRequest object as input.
- Log the start of the operation, including the member code.
- Validate that memberCode is not null or empty; if it is, return a CommonResponse error with HTTP 400 and a proper error message.
- Fetch all successful face scan results for the member using faceScanResultRepository.findByMemberCodeAndScanResultOrderByEndTimeDesc.
- If no results are found, return a CommonResponse error with HTTP 404 and a proper error message.
- Map the results into FaceScanHistoryDto.ScanRecord objects using a stream. Handle parsing errors gracefully by creating a basic scan record for problematic entries.
- Wrap the mapped scan records into a FaceScanHistoryDto with a success message.
- Log the successful retrieval and return the CommonResponse.success with the DTO.
- Handle DataAccessException and other exceptions by logging the errors and returning CommonResponse.error with HTTP 500 and appropriate error messages.

Repository:-
create a Spring Data JPA repository interface for StoreFaceScanResultModel named FaceScanResultRepository.
Implement a method findByMemberCodeAndScanResultOrderByEndTimeDesc in FaceScanResultRepository.
Requirements:
- The method should accept memberCode (String) and success (String) as parameters.
- It should return a List<StoreFaceScanResultModel> containing all scan results for the given memberCode where scanResult equals success.
- The results should be ordered by endTime in descending order.
- Use Spring Data JPA method naming conventions (no @Query annotation needed).

