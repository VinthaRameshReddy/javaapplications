@auth-service/  for this service we want to change the functionality for checking purpose like we will create one table with member codes like if the member code is in that table for that member codes otp verification should be skipped it should be like default value 12345 if the users form that members table for the otp verification if they enter 12345 it should be validated at the time of registration and the login also