API:-/faceScan-service/api/v1/faceScan/storeResult-->create api based on this prompt

Entity:-
- Create a class class named `StoreFaceScanResultModel` mapped to the `MEDGO_FACE_SCAN_RESULTS` table.
- The class should extend `BaseEntity` to inherit common auditing fields and primary key handling.
- Annotate the class with `@Entity` and `@Table(name = "MEDGO_FACE_SCAN_RESULTS")`.
- Include the following fields with proper JPA annotations:
    - `sessionId` of type `String`, annotated with `@Column(name = "SESSION_ID", nullable = false, unique = true)`, representing a unique session ID.
    - `fedId` of type `String`, representing the federated membership code.
    - `memberCode` of type `String`, representing the associated member code.
    - `scanResult` of type `String`, representing the scan result (e.g., "SUCCESS", "FAILED").
    - `scanData` of type `String`, annotated with `@Column(name = "SCAN_DATA", columnDefinition = "TEXT")`, storing JSON as a string.
    - `endTime` of type `LocalDateTime`.
    - `measurementID` of type `String`, annotated with `@Column(name = "MEASUREMENT_ID")`.
- Include a method `getScanDataAsJson()` to convert `scanData` JSON string to `Map<String, Object>` with error handling and logging.
- Include a constructor that accepts `fedId` and `memberCode`.
- Use Lombok annotations `@Data`, `@NoArgsConstructor`, `@AllArgsConstructor`, `@ToString` and logging annotation `@Slf4j`.
- Ensure all fields and inherited properties are correctly mapped to the database columns.

face scan session model entity:-
Entity: 
- Create a class named `FaceScanSessionModel` mapped to the `MEDGO_FACE_SCAN_SESSION` table.
- The class should extend `BaseEntity` to inherit common auditing fields and primary key handling.
- Annotate the class with `@Entity` and `@Table(name = "MEDGO_FACE_SCAN_SESSION")`.
- Include the following fields with proper JPA annotations:
    - `fedId` of type `String`, annotated with `@Column(name = "FED_ID", nullable = false, unique = true)`.
    - `memberCode` of type `String`, annotated with `@Column(name = "MEMBER_CODE")`.
    - `sessionId` of type `String`, annotated with `@Column(name = "SESSION_ID")`.
    - `consent` of type `boolean`, annotated with `@Column(name = "CONSENT")`.
    - `creationTime` of type `LocalDateTime`, annotated with `@Column(name = "CREATED_TIME")`.
    - `age` of type `Integer`, annotated with `@Column(name = "AGE")`.
    - `memberName` of type `String`, annotated with `@Column(name = "MEMBER_NAME")`.
    - `gender` of type `String`, annotated with `@Column(name = "GENDER")`.
    - `weight` of type `Double`, annotated with `@Column(name = "WEIGHT")`.
    - `height` of type `Double`, annotated with `@Column(name = "HEIGHT")`.
    - `isSmoker` of type `String`, annotated with `@Column(name = "IS_SMOKER")`.
    - `isDiabetic` of type `String`, annotated with `@Column(name = "IS_DIABETIC")`.
    - `bpMedication` of type `String`, annotated with `@Column(name = "BP_MEDICATION")`.
    - `sessionEndTime` of type `LocalDateTime`, annotated with `@Column(name = "SESSION_END_TIME")`.
    - `status` of type `String`, annotated with `@Column(name = "STATUS")`.
- Include a constructor that accepts `fedId` and `memberCode`.
- Use Lombok annotations `@Data`, `@NoArgsConstructor`, `@ToString` for boilerplate reduction.
- Ensure all fields and inherited properties are correctly mapped to the database columns.

Request:-
create a request class named FaceScanResultRequest in the request package.
Implement a request DTO FaceScanResultRequest to receive and validate input for storing face scan results.

Requirements:
- Annotate the class with Lombok annotations @Data and @ToString.
- Extend the base class BaseDto.
- Include the following fields with validation:
- sessionId of type String, annotated with @NotBlank(message = "Session ID is required").
- scanResult of type String, annotated with @NotBlank(message = "Scan result is required").
- scanData of type Map<String, Object> to hold JSON data (optional).
- endTime of type String, annotated with @NotBlank(message = "End time is required"), which will later be parsed into LocalDateTime.
- This class will be used in the controller to receive and validate the scan result data before storing it in the database.

Controller:-
 create an endpoint method for storing face scan results.
"Give me a Spring Boot controller method named storeFaceScanResult mapped to the endpoint /storeResult using the @PostMapping annotation. The method should use @DecryptBody(FaceScanResultRequest.class) to decrypt and validate the incoming request body of type FaceScanResultRequest, and @EncryptResponse to encrypt the response. It should call the storeFaceScanResult method from the faceScanService and return a CommonResponse. The method should also declare that it throws JsonProcessingException."


Service Interface:-
create a service interface for handling face scan operations.
"Give me a Spring Boot service interface named FaceScanService that declares a method storeFaceScanResult accepting a @Valid FaceScanResultRequest object as a parameter and returning a CommonResponse. The method should also declare that it throws a JsonProcessingException. This method will be used to handle and process the storing of face scan results."


serviceimpl:-
create impl package in the package and implement the method inside the implementation class.
Give me the full implementation of a FaceScanServiceImpl class that implements the FaceScanService interface and defines the method storeFaceScanResult(FaceScanResultRequest request).

The implementation should:
-Log the start of the process for storing the face scan result.
-Validate that sessionId is not null or empty. If it is, return a CommonResponse.error with an appropriate error message and HTTP 400 code.
-Retrieve the session using FaceScanSessionRepository.findBySessionId(sessionId). If not found, return an error response with a 404 status.
-Parse the endTime string into a LocalDateTime using DateTimeFormatter.ISO_DATE_TIME. If parsing fails, return a CommonResponse.error with an invalid date format message.
-Check if a StoreFaceScanResultModel already exists using faceScanResultRepository.findBySessionId(sessionId).
-If it exists, update it.
-If it doesn’t, create a new one and populate fields like sessionId, fedId, and memberCode from the session.
-Set additional fields including scanResult, measurementID, and scanData.
-Serialize scanData using objectMapper.writeValueAsString(request.getScanData()). Handle JsonProcessingException gracefully and return an appropriate error response if serialization fails.
-Save the result to the faceScanResultRepository.
-Update the FaceScanSessionModel entity with sessionEndTime, updatedOn, and status as "COMPLETED", then save it in the repository.
-Return a success response (CommonResponse.success("Face scan result stored successfully")) once the process completes successfully.

Catch and handle DataAccessException for database issues and general Exception for unexpected errors, logging each appropriately and returning a relevant error response.
Annotate the method with @Transactional and @Override, and ensure proper logging with log.info, log.debug, and log.error messages throughout the method.


Repository:-
FaceScanSessionRepository:-
- Create a Spring Data JPA repository interface named FaceScanSessionRepository in the repository package.
- The interface should:
- Extend JpaRepository<FaceScanSessionModel, Long>.
- Include a custom method findBySessionId(String sessionId) that returns an Optional<FaceScanSessionModel>.
- Annotate the method parameter sessionId with @NotBlank(message = "Session ID is required") to enforce validation.
- Be annotated with @Repository.
- This repository will be used to retrieve face scan session records by their unique session ID.

FaceScanResultRepository:-
- Create a Spring Data JPA repository interface named FaceScanResultRepository in the repository package.
- The interface should:
- Extend JpaRepository<StoreFaceScanResultModel, Long>.
- Include a custom method findBySessionId(String sessionId) that returns an Optional<StoreFaceScanResultModel>.
- Be annotated with @Repository.
- This repository will be used to retrieve face scan result records associated with a specific session ID.



