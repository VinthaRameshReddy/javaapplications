Service: reimbursement-service
Controller: ReimbursementController
Repo path: services/reimbursement-service

Goal:
Regenerate the complete API implementation for endpoints in `ReimbursementController`.
Return a full patch or file set with code, tests, and resources required to build the service.

Requirements:
- Java 17, Spring Boot 3.x
- Preserve package names, existing configs, and build files.
- Ensure behavior parity with current endpoints and DTOs.

Output format:
- Git-style patch or enumerated files with full contents. Highlight breaking changes.

Now regenerate the implementation for this controller and related service layers.
