Service: shared-provider-service
Controller: HealthFacilityController
Original prompt: ramesh-shared-provider-healthfacilitycontroller.md
Scope: Regenerate the entire `shared-provider-service` centered on health facility management endpoints.

Instructions:
1. Parse `services/shared-provider-service` for controller method signatures, DTOs, repositories, and services.
2. Implement controllers, service layer, JPA repositories (or existing persistence), DTOs, mapping, validation, and tests.
3. Include caching and search/filter capabilities where applicable; provide pagination on list endpoints.

Requirements:
- Java 17, Spring Boot 3.x
- Preserve package names and integration points with other services.
- Include unit and integration tests.

Output:
- Provide a git-style patch with full contents for all modified/added files.

Regenerate now.
