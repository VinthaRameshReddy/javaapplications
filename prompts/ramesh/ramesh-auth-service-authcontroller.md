Service: auth-service
Controller: AuthController
Repo path: services/auth-service

Goal:
Regenerate the API for authentication endpoints in `AuthController` (login, logout, session, etc.).
Produce complete code, security configuration, DTOs, and unit tests.

Requirements:
- Java 17, Spring Boot 3.x, existing security patterns (JWT, token generation) must be preserved.
- Keep package structure and integration points with other services.

Output format:
- Provide a git-style patch or full file set with explanations for any security changes.

Regenerate the auth service endpoints now.
