Service: auth-service
Controller: GenerateTokenController
Repo path: services/auth-service

Goal:
Regenerate the token-generation endpoints and any supporting utilities defined in `GenerateTokenController`.
Return complete implementation, including token service, security configs, and tests.

Requirements:
- Java 17, Spring Boot 3.x
- Ensure tokens follow existing formats and expiration policies.

Output:
- Git-style patch or detailed file list with contents.

Regenerate now.
