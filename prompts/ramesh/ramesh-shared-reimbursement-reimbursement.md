Service: shared-reimbursement-service
Controller: ReimbursementController
Repo path: services/shared-reimbursement-service

Goal:
Regenerate the complete API implementation for the endpoints defined in `ReimbursementController`.
The output should be a full, ready-to-apply git-style patch (or files) containing all updated/added Java source files, tests, and any required resources.

Requirements:
- Java 17, Spring Boot 3.x
- Preserve package names and existing project structure.
- Keep existing configuration and annotations (encryption/decryption, validation, logging).
- Include controllers, services, DTOs, document handling, and unit tests for core logic.
- Ensure multipart handling and file validations are preserved and robust.

Endpoints (from controller):
- GET /v1/history?memberCode=
- POST /v1/viewAmount
- POST /v1/submitReimbursement (multipart/form-data)
- POST /v1/resubmitReimbursement (multipart/form-data)
- GET /v1/images/reimbursement-success.jpg

Output format:
- Provide a git-style patch or a list of files with full contents. For each file include path, language, and code. Mark breaking changes explicitly.

Now regenerate the full implementation for this service.
Service: shared-reimbursement-service
Controller: ReimbursementController
Repo path: services/shared-reimbursement-service

Goal:
Regenerate the complete API implementation for the endpoints defined in `ReimbursementController`.
The output should be a full, ready-to-apply git-style patch (or files) containing all updated/added Java source files, tests, and any required resources.

Requirements:
- Java 17, Spring Boot 3.x
- Preserve package names and existing project structure.
- Keep existing configuration and annotations (encryption/decryption, validation, logging).
- Include controllers, services, DTOs, document handling, and unit tests for core logic.
- Ensure multipart handling and file validations are preserved and robust.

Endpoints (from controller):
- GET /v1/history?memberCode=
- POST /v1/viewAmount
- POST /v1/submitReimbursement (multipart/form-data)
- POST /v1/resubmitReimbursement (multipart/form-data)
- GET /v1/images/reimbursement-success.jpg

Output format:
- Provide a git-style patch or a list of files with full contents. For each file include path, language, and code. Mark breaking changes explicitly.

Now regenerate the full implementation for this service.
