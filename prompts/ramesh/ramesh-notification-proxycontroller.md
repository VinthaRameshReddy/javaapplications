Service: notification-service
Controller: NotificationProxyController
Repo path: services/notification-service

Goal:
Regenerate notification proxy endpoints implemented in `NotificationProxyController`.
Include transport logic, DTOs, error handling, and tests.

Requirements:
- Java 17, Spring Boot 3.x
- Preserve messaging/integration patterns with downstream services.

Output:
- Git-style patch or file list with full contents.

Regenerate now.
