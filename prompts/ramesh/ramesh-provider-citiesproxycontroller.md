Service: provider-service
Controller: CitiesProxyController
Repo path: services/provider-service

Goal:
Regenerate the proxy controller for city lookups and proxied provider endpoints in `CitiesProxyController`.
Include proper error handling, caching, and tests for proxy logic.

Requirements:
- Java 17, Spring Boot 3.x
- Preserve external API contracts and Feign/RestTemplate usage where applicable.

Output:
- Git-style patch or file list with full contents.

Regenerate now.
