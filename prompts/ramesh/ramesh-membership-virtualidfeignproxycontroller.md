Service: membership-service
Controller: VirtualIdFeignProxyController
Repo path: services/membership-service

Goal:
Regenerate the Feign proxy endpoints for virtual ID interactions defined in `VirtualIdFeignProxyController`.
Return full code, error handling, and tests.

Requirements:
- Java 17, Spring Boot 3.x
- Preserve Feign client contracts and request/response schemas.

Output:
- Git-style patch or file list with contents.

Regenerate now.
