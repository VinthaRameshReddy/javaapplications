Service: shared-provider-service
Controller: CityController
Original prompt: ramesh-shared-provider-citycontroller.md
Scope: Regenerate `shared-provider-service` implementing city-related endpoints fully.

Instructions:
1. Scan `services/shared-provider-service` to extract controller methods, DTOs, services, and repositories.
2. Implement controller handlers, service layer, DTOs, and persistence with proper validation and tests.
3. Provide paging, filtering, and caching for city lists; ensure correct response shapes.

Requirements:
- Java 17, Spring Boot 3.x
- Preserve existing package structure and integrations.
- Include unit and integration tests.

Output:
- Git-style patch with all updated/added files and migration notes.

Regenerate now.
