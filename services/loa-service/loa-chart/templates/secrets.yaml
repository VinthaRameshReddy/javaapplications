apiVersion: v1
kind: Secret
metadata:
  name: {{ include "loa-chart.name" . }}-creds
  labels:
    app: {{ include "loa-chart.name" . }}
type: Opaque
data:
  application.properties: |
{{ .Values.appProperties | b64enc | indent 4 }}
