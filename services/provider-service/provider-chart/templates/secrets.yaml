
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "provider-chart.name" . }}-creds
  labels:
    app: {{ include "provider-chart.name" . }}
type: Opaque
data:
  application.properties: |
{{ .Values.appProperties | b64enc | indent 4 }}
