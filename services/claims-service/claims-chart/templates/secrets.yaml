apiVersion: v1
kind: Secret
metadata:
  name: {{ include "claims-chart.name" . }}-creds
  labels:
    app: {{ include "claims-chart.name" . }}
type: Opaque
data:
  application.properties: |
{{ .Values.appProperties | b64enc | indent 4 }}
