apiVersion: v1
kind: Secret
metadata:
  name: {{ include "filemanagement-chart.name" . }}-creds
  labels:
    app: {{ include "filemanagement-chart.name" . }}
type: Opaque
data:
  application.properties: |
{{ .Values.appProperties | b64enc | indent 4 }}

