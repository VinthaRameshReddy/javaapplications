apiVersion: v1
kind: Secret
metadata:
  name: {{ include "shared-utilization-chart.name" . }}-creds
  labels:
    app: {{ include "shared-utilization-chart.name" . }}
type: Opaque
data:
  application.properties: |
{{ .Values.appProperties | b64enc | indent 4 }}
