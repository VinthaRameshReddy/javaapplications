
replicaCount: 1

name: shared-membership
fullname: shared-membership-service

image:
  repository: acrmpiseaashared01.azurecr.io/shared-membership-service-stg
  tag: latest
  pullPolicy: IfNotPresent
  pullSecret: medgopullsecret

service:
  type: ClusterIP
  port: 80
  targetPort: 14488

resources:
  requests:
    cpu: "250m"
    memory: "1200Mi"
  limits:
    cpu: "500m"
    memory: "2400Mi"

env:
  ##javaToolOptions: "-Xss16m"
  name: "application-stg.properties"

pvcName: shared-stg-pvc

hpa:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  memoryUtilization: 80

# appProperties: |
#   db.username=demouser
#   db.password=demopass
#   spring.profiles.active=dev



