apiVersion: v1
kind: Secret
metadata:
  name: {{ include "communication-chart.name" . }}-creds
  labels:
    app: {{ include "communication-chart.name" . }}
type: Opaque
data:
  application.properties: |
{{ .Values.appProperties | b64enc | indent 4 }}
