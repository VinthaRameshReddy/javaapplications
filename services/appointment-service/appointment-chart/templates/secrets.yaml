apiVersion: v1
kind: Secret
metadata:
  name: {{ include "appointment-chart.name" . }}-creds
  labels:
    app: {{ include "appointment-chart.name" . }}
type: Opaque
data:
  application.properties: |
{{ .Values.appProperties | b64enc | indent 4 }}
