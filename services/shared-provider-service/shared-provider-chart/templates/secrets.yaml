
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "shared-provider-chart.name" . }}-creds
  labels:
    app: {{ include "shared-provider-chart.name" . }}
type: Opaque
data:
  application.properties: |
{{ .Values.appProperties | b64enc | indent 4 }}
