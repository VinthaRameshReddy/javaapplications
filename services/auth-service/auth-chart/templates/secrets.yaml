apiVersion: v1
kind: Secret
metadata:
  name: {{ include "auth-chart.name" . }}-creds
  labels:
    app: {{ include "auth-chart.name" . }}
type: Opaque
data:
  application.properties: |
{{ .Values.appProperties | b64enc | indent 4 }}
