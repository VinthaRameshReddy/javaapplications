apiVersion: v1
kind: Secret
metadata:
  name: {{ include "membership-chart.name" . }}-creds
  labels:
    app: {{ include "membership-chart.name" . }}
type: Opaque
data:
  application.properties: |
{{ .Values.appProperties | b64enc | indent 4 }}

