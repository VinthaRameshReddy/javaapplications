replicaCount: 1

name: reimbursement
fullname: reimbursement-service

image:
  repository: acrmpiseaashared01.azurecr.io/reimbursement-service-stg
  tag: latest
  pullPolicy: IfNotPresent
  pullSecret: medgopullsecret

service:
  type: ClusterIP
  port: 80
  targetPort: 10087

resources:
  requests:
    cpu: "250m"
    memory: "1200Mi"
  limits:
    cpu: "500m"
    memory: "2400Mi"

env:
  ##javaToolOptions: "-Xss16m"
  name: "application.properties"

pvcName: medgo-logs-pvc

hpa:
  enabled: true
  minReplicas: 1
  maxReplicas: 3
  memoryUtilization: 80

# appProperties: |
#   db.username=demouser
#   db.password=demopass
#   spring.profiles.active=dev

