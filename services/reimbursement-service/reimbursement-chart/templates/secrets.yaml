apiVersion: v1
kind: Secret
metadata:
  name: {{ include "reimbursement-chart.name" . }}-creds
  labels:
    app: {{ include "reimbursement-chart.name" . }}
type: Opaque
data:
  application.properties: |
{{ .Values.appProperties | b64enc | indent 4 }}

