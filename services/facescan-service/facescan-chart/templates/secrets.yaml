apiVersion: v1
kind: Secret
metadata:
  name: {{ include "facescan-chart.name" . }}-creds
  labels:
    app: {{ include "facescan-chart.name" . }}
type: Opaque
data:
  application.properties: |
{{ .Values.appProperties | b64enc | indent 4 }}

